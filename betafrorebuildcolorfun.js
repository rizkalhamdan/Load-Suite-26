// function dqws(n) {
// 	localStorage.colors = localStorage ?? 0 // Changed to system
// 	localStorage.colors = n%2 == 0 ?0:1
// 	n = localStorage.colors

// 	var root = document.querySelector('body').style
// 	var vams = ["--background", "--second-col", "--opacit-col", "--text-color", "--chat-color"]

// 	for (let i = 0; i < vams.length; i++) {
// 		const e = vams[i];
// 		root.setProperty(e, e + n%2 == 0 ?"-dk":"wh")
// 	}
// }


// function gwrsv(n) {
//     localStorage.colors = localStorage.colors ?? 1
//     if (!(n == 1 || n == 2 || n == 3 || n == 4)) n = localStorage.colors
//     localStorage.colors = n
    
    
//     var root = document.querySelector('body').style,
//       nams = ["-dk", "-wh", "-dk-tow", "wh-tow"],
//       vams = ["--background", "--second-col", "--opacit-col", "--text-color", "--chat-color"] // Dont vhange before go to Naghhom .. #head
    
//     for (var i = 0; i < vams.length; i++) {
//       root.setProperty(vams[i], `var(${vams[i]}${nams[n-1]})`)
//     }
//   }


